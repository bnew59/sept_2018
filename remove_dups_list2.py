def duplicates(dups):
    final_list = []
    for num in list:
        if num not in final_list:
            final_list.append(num)

    return final_list

first_list = [1, 2, 3, 4, 2, 3]

print(duplicates(first_list)) 


